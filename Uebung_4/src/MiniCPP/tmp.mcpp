void main(){
    int n;
    cout << "n > ";
    cin >> n;
    if (n > 2)
        Sieve(n);
}

/**
void Sieve(int n); // declaration
void main() {
    int n;
    cout << "n > ";
    cin >> n;
    if (n > 2)
    Sieve(n);
} // main

void Sieve(int n) { // definition
    int col, i, j;
    bool *sieve = 0;
    sieve = new bool[n + 1];
    i = 2;
    while (i <= n) {
        sieve[i] = true;
        i++;
    } // while
}
*/